
#include <stdlib.h>
#include <string.h>

#include "linkedlist.h"
#include "textfilewriter.h"

void read_command(char** cmds);

typedef struct {
    int snum;
    char dept[30];
    char name[30];
} Student;

int main() {
    create_music_titles(NULL);
    printf("Hello World!!");
    return 0;
}

void read_command(char** cmds) {
    
}

